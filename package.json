{
  "name": "qrcode-hassle-free",
  "version": "1.0.0",
  "private": true,
  "description": "Prints a scannable QR code + cloudflared access link to the dsh web terminal on every start. No database, no Firebase.",
  "type": "module",
  "main": "lib/index.js",
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
